#include<stdio.h>
#include<string.h>
int main()
{
  int i,n;
  char s[10000];
  scanf("%d",&n);
  for(i=0;i<=n;i++){
      scanf("%c",&s[i]);
  }
  int cal=0;
  for(i=0;s[i]!='\0';i++){
      if(s[i]==s[i+1]){
            cal++;
      }
  }
  printf("%d\n",cal);
}
