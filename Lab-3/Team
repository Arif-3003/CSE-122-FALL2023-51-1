#include<stdio.h>
int main()
{
      int a,b,c,n,i,cnt=0;
      scanf("%d",&n);
      for(i=0;i<n;i++){
      scanf("%d %d %d",&a,&b,&c);
      if(a+b+c>=2)
            cnt++;
      }
      printf("%d",cnt);
      return 0;
}
